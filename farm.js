// Import cox-chain dependency
const coxChain = require('cox-chain');

// Log Web3 version
console.log('Web3 version:', coxChain.Web3.version);

// Use cox-chain dependency
console.log('cox-chain version:', coxChain.version);
